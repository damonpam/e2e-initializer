/*
Given('I am in the {word} {int} category page', async function (category: string, foo: number, table: TableDefinition) {
  // TODO
});

When(
'I navigate to the product page of {ProductSKU}', async function (productSKU: string) {
  // TODO
});

Then('I should see the product not found page', async function () {
  // TODO
});

Then(/^the (?:object|model) ([^"].*) should (not )?contain one element with the following properties:$/, async function (
  model: string,
  notContain: boolean,
  table: TableDefinition
) {
  // TODO
});

Then(
  'the product with EAN {productEAN} should not be published from Mirakl',
  { timeout: TWO_MIN_IN_MS },
  async function (ean: string) {
  // TODO
});

Then(
  /^the user should( not)? have a saved card for the next checkout with the following (?:product|products)$/,
  async function (notSaved: string, table: TableDefinition) {
  // TODO
});
*/
