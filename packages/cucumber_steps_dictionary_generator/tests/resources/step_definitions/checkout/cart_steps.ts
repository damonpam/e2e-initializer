/*
Then('the cart should be stored with the same address info for all products and sellers', async function () {
  // TODO
}

Then('I should see {int} {float} product(s) in the {boolean} cart', async function (quantity: number, foo: number, baz: boolean) {
  // TODO
});

Then('I should see {int} unit(s) for {ProductSKU} product in the cart', async function (
  quantity: number,
  productSKU: string
) {
  // TODO
});
*/
